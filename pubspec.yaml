name: common_locale_data
description: A type-safe and tree-shakable API to access translated common data (measurement units, date fields, country names etc...)
version: 1.0.0

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  collection:
  intl:

dev_dependencies:
  build_runner:
  dart_style:
  http:
  json_annotation:
  json_serializable:
  lints:
  process_runner:
  test:
